import { RecordsFilterPipe } from './records-filter.pipe';

describe('RecordsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecordsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
